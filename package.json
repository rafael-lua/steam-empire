{
  "name": "steam-empire",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "pushdist": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "deploy": "npm run generate && npm run pushdist"
  },
  "dependencies": {
    "core-js": "^3.9.1",
    "nuxt": "^2.15.3",
    "progressbar.js": "^1.1.0",
    "push-dir": "^0.4.1"
  }
}
